# Phase 14 — Specification Contradiction Log (M0)

*Status: open. Per the implementation brief's critical rule: when implementation reveals a genuine contradiction in the frozen specification, work stops at that point, the contradiction and its smallest resolution are documented here, and nothing is silently redesigned. Entries below were found during M0 extraction (pre-implementation) and resolved at the smallest possible scale without touching the frozen grammar.*

| # | Finding | Where | Kind | Smallest resolution | Status |
|---|---|---|---|---|---|
| G-1 | `repeat for each word, position in words` (§7.5) needs a concrete M0 pattern form; §7.15's `pattern` is shared with `match` (M1) and includes constructs M0 does not have. | 00 §7.5 / §7.15 | Underspecification (not a contradiction) | M0: pattern = name, plus the two-name index form as its own production. Full patterns remain M1 with `match`. | Resolved (docs/13 §4) |
| G-2 | `decimal` printing format is explicitly deferred to doc 03 ("spec at M1") but the calculator validation project must print decimals at M0. | 03-semantics open questions | Underspecification | M0: shortest-roundtrip (Rust f64 `{}`); provisional until doc 03 freezes. | Resolved (docs/13 §4) |
| G-3 | `repeat n times using i` with a variable `n` is not derivable from `count = number "times"` — and the stress-test write-ups assume it. | 00 §7.15 vs stress-test programs | Grammar/examplar tension | **Frozen grammar wins:** M0 implements the literal form only; variable counts are expressed with `repeat while` + a mutable counter (spec-faithful, taught in the validation programs). No grammar extension was made. | Resolved (docs/13 §4) |
| G-4 | Failing `check that` message rendering is unspecified. | 5.1 / 26.3 | Underspecification | Rendered as `check failed: <comparison> (left = …, right = …)` with span; snapshot-tested. | Resolved (docs/13 §4) |
| G-5 | Multi-line string literals (§7.7) conflict with the continuation rule (§7.0.1): the doc's `make long equal to "line one⏎line two"` example has the second line at the *same* indent, which 7.0.1 forbids for statement continuation — yet the string is not statement syntax. | 00 §7.0.1 vs §7.7 | Contradiction between two normative rules | Smallest resolution: an *open string literal* continues onto the next line regardless of indentation (string content is verbatim; no indent stripping — which also resolves doc 02's pending "multi-line string indentation semantics" question). Statement continuation proper keeps the 7.0.1 deeper-indent rule unchanged. | Resolved (lexer; documented in docs/13 §4) |
| G-6 | The continuation rule's "incomplete" tail test needs a word list. The type words (`number`, `text`, …) must NOT be in it: `function calculate score⏎    takes number of correct answers` has `takes` as the tail word (correct), but a `make x equal to number⏎`-style line must end the statement when the head is complete. | 00 §7.0.1 | Underspecification (implementation detail of 7.0.1) | Tail-word list frozen in the lexer (`INCOMPLETE_TAIL_WORDS`), pinned by tests; type words excluded. This is a formalization of 7.0.1's "completeness is a property of the token at the line end", not a grammar change. | Resolved (lexer) |
| G-7 | Header lines must never continue, but `repeat 10 times…` ends in `times` — an incomplete-tail word — so the lexer joined the header with its (deeper-indented) body line. Likewise `can fail` ends in `fail` and `structure player⏎    has …` is safe, but the general rule is unstated. | 00 §7.0.1 vs §7.4/§7.5/§7.8 | Contradiction between the continuation rule and single-line-header forms | Smallest resolution: `times` and `fail` are removed from the tail-word list — `times` in expression position is always an *infix* operator (an operand precedes it), never a statement tail; bare `fail` is a diagnostic fragment (`fail with` lines end in `with`, which stays). Pinned by parser tests (`repeat 10 times` / `can fail` clause). No grammar change. | Resolved (lexer; docs/13 §4) |
| G-8 | The articles `a`/`an` are keywords, but §7.9's own example `bigger of a and b` uses `a` as a *variable name*; §7.11's construction `a player with …` uses `a` as the article. The grammar cannot have both under a closed keyword set unless articles are contextual. | 00 §7.9/§7.11 vs §7.0.3 | Examplar-vs-keyword tension (not a grammar change) | Articles are contextual keywords: article-in-type-or-construction position, name elsewhere. The parser disambiguates locally (`a` followed by a name ⇒ construction; otherwise a one-word name). Recorded here because §7.0.3's "names may not collide with keywords" needs this one explicit exception. | Resolved (parser; docs/13 §4) |
| G-9 | The `math` module's function signatures are unspecified: §7.13/S-11 show `use math for square root, floor` but no document states their parameter or result types. M0 sema needs them. | 00 §7.13 / docs/13 S-7/S-11 | Underspecification | Smallest M0 table: `square root of n` : number → decimal (D-7's promotion family — irrational results), `floor of n` : decimal → number (its inverse pair); both pure. Related note: the `divide` of §13.1 is a teaching example, not a builtin — validation programs declare it as a user `can fail` function. | Resolved (sema; docs/13 §4) |
| G-10 | §13.1 says the `if it fails/otherwise` attempt form "evaluates to the branch's value", but frozen §7.15 places attempt tails in *statement* position only (R-20.1's expression form is the bare attempt), so `make x equal to attempt … if it fails …` is not derivable. | 00 §13.1 vs §7.15 | Examplar-vs-grammar tension (frozen grammar wins, as in G-3) | M0 form uses the fixed `result` binding (13.1's own mechanism): `attempt … if it fails then … otherwise make x equal to result`. Expression-position tails stay M1+. No grammar change. | Resolved (sema; docs/13 §4) |

| G-11 | §7.10's own example `make changing attempts equal to 0 of type number` uses an `of type` annotation suffix, but docs/13 §1's `make` production (`make = "make" ["changing"] name "equal to" expr`) cannot derive it — the line dies at end-of-line. | 00 §7.10 vs docs/13 §1 | Examplar-vs-grammar tension (as G-3) | `of type <type>` is accepted as an optional suffix of `make` (the annotation is examplar-normative; §7.10's whole point is that annotations are *optional*, so omitting it stays valid). No grammar redesign — one optional tail added to the extract's production. | Resolved (parser; docs/13 §1) |

| G-12 | The continuation rule's tail-word list (G-6) still contained the articles `a`/`an`, but G-8 makes articles contextual *names* — `takes number called a` ends a legal clause line with `a` and was wrongly joined to the next line. | 00 §7.0.1 vs §7.9 (G-8) | Contradiction between two normative rules (continuation rule vs contextual articles) | Articles leave the tail-word list: every documented article-led type phrase (`a list of`, `a map from`, `a pair of`, `a player with`) carries its continuation within the line, and `of`/`with` remain tail words for the genuinely incomplete spellings. Pinned by sema test (`takes number called a` clause). | Resolved (lexer) |

| G-14 | §13.1's normative example declares `divide` with `returns a number` and `gives back top divided by bottom` — but D-10/§8.2 rule that `divided by` promotes to decimal, so the example is a compile error under the spec's own type rules (the annotated-`number`-receiving-decimal case). | 00 §13.1 example vs D-10/§8.2 | Contradiction between the error-model example and the number decision | D-10 wins (it is the deliberate, load-bearing decision: one number type, promotion on `divided by`, explicit `divided evenly by` escape). The example's signature must read `returns a decimal` (or the body use `divided evenly by`); sema implements D-10 exactly and the spec example is flagged for the one-word correction at the next doc pass. | Resolved (sema; spec example flagged) |
| G-15 | §7.0.3's greedy call-split rule splits a word run into head + tail (`say result` → `say(result)`) but does not say what span the synthetic tail argument gets. Using the whole run's span collides with the call node in any span-keyed side table (the node-type table HIR reads), silently mis-typing the argument. | 00 §7.0.3 vs §26.5 (span-keyed provenance) | Underspecification (implementation detail of 7.0.3) | The tail words occupy the end of the run in the source, so the tail's sub-span is computable exactly (helper in sema); the synthetic argument and any inner wrapped call take the tail words' own span, never the outer call's. Applies to both split sites (name-split and callee-split). No grammar change. | Resolved (sema) |
| G-16 | §7.15's `call = name [arg1] {prep argN} …` production gives no AST home distinguishing prepositional reads from positional args, and §7.6's index read (`things at 2`) is spelled with the preposition `at`. Implementations must agree on where an `at`-read's index lives (prepositional list, not the first positional slot) or flowing reads mis-lower to calls. | 00 §7.6/§7.15 | Underspecification (AST-shape clarification) | Prepositional arguments (`of/at/from/to`) are stored in the `preps` list in source order; the first positional slot holds only argument-one (`divide 10`). HIR's read classification looks up `at` in `preps` accordingly. Pinned by the `list_and_index_read` MIR test. No grammar change. | Resolved (hir) |
| G-17 | §13.1's `attempt … as <name>` gives the bound name no span in the AST clause, but MIR needs one for the local's provenance (26.5: every value event names its line). | 00 §13.1 vs §26.5 | Underspecification | The `as` tail carries the bound name's span from the parser's `Name` node through sema and HIR to the MIR local. No grammar change. | Resolved (sema/hir) |
| G-18 | §7.9's pair form (`a pair of A and B`, §7.6 type row) defines no element accessor: `first of` is specified over lists only (S-13), and §7.6's `last of` is unmentioned for pairs. An M0 program cannot read a pair's parts without M1 destructuring. | 00 §7.6/§7.9 vs docs/13 S-13 | Underspecification (M0 feature gap) | Recorded, not silently redesigned: at M0 pairs are constructed and printed (S-9) but not indexed; element access joins the destructuring/M1 design rather than inventing a second accessor here. The interpreter implements the MIR surface exactly. | Open — flagged for the doc pass |
| G-19 | §22.2's `pad_on_fail` gives instruction-level failures a routing edge, but the spec does not say whether an index-out-of-range read inside an `attempt` is a crash (trap) or a failure (catchable). The interpreter must pick one; programs differ observably. | 00 §22.2 vs §13.1 (honesty rule) | Underspecification (error-model boundary) | Inside an attempt, out-of-range is a *failure* — it flows to the pad exactly like `fail with` (13.1's "the runtime tells the truth" rule); outside any attempt it is a crash with the teaching trap. Pinned by `index_out_of_range_is_a_failure_inside_attempt`. | Resolved (interp; docs/13 §4 candidate) |
| G-20 | §8.4 (text row) says concatenation is `+`/`plus`, but the arithmetic rule (7.3, S-1) says arithmetic works on numbers — M0's checker rejected `text plus text` as E0357, while §7.7's interpolation exists for mixed text. Two normative sentences conflict for the M1 case. | 00 §8.4 (text row) vs §7.3/S-1 | Contradiction between two normative type rules | §8.4's normative sentence wins (it is the type-row rule, and `+` is already the school-math symbol students know from strings): **`text plus text` concatenates**. Mixing text with any non-text operand stays an error (interpolation is the teaching form — 7.7). The E0357 diagnostic carves out the text/text case; pinned by sema tests. | Resolved (sema; docs/13 §4 candidate) |

*No further entries yet. Any implementation-stage contradiction appends here and pauses that work item until resolved.*

## M1-stage entries

| # | Finding | Where | Kind | Smallest resolution | Status |
|---|---|---|---|---|---|
| G-21 | §8.5's normative sentence "Values: `nothing` and `something with value v`" defines the option *construction expression*, but §7.15's expression grammar cannot derive `something with value v` — `primary` has no option-literal production (only the `pattern` production carries the phrase). A function `returns a number or nothing` cannot produce its `something` case in a way §7.15 derives. | 00 §8.5 vs §7.15 | Examplar-vs-grammar tension (the §8.5 value sentence is exemplar-normative; the pattern production proves the phrase's shape) | §8.5's value sentence wins (it is the type's own definition, and the *pattern* production already freezes the phrase-token). `something with value <additive>` is legal as an expression — the option construction, mirroring `nothing` (a literal) and `a pair of … and …` (pairlit). One production added to the extract: `optionlit = "nothing" \| "something with value" additive`. R-4's additive bound keeps `and` an argument separator inside it. The inner value lowers to itself (options are value-or-`nothing` at runtime, D-34): `something with value v` *is* `v`, so no new MIR instruction and both backends agree for free. Pinned by parser/sema/interpreter/native tests. | Resolved (parser/sema/hir; docs/13 §4 candidate) || G-22 | `flowcall = name [additive] { prep additive }` is a greedy ordered read, but the grammar's own exemplars (`split text and separator`, `write file content at path`) put the callee's LAST word immediately before the first argument (`split line and ","`, `write file "hi" at "p"`). A plain `additive` read of `line and …` stops at `and` and leaves `","` unreachable as the second positional, and `write file "hi" at "p"` then mis-splits. The parse is only derivable when the first positional argument is read from `primary` (its value rides the first-argument slot) and the remaining additive continues in `additive` — the additive-level argument rule (R-4) already makes this the only consistent reading: an argument's FIRST word is part of the same value run as its callee tail. | 00 §7.15 `flowcall`/R-4 vs §7.15's own call exemplars | Grammar-exemplar tension (same G-11/G-21 class: the frozen exemplars are normative, the production's naive reading under-derives them) | The first positional argument of a `flowcall` binds at `primary` level when a prep or `and` continuation follows the value's head word — exactly the reading `write file content at path` forces. One clarification added to the extract: `flowcall = name [primary-continuation] { prep additive } { "and" additive }` (the head word of the first positional argument is greedy; the rest of its additive continues normally). Deterministic: no backtracking; the continuation is chosen before any parse commitment. Pinned by parser tests (`split … and …`, `write file … at …` in flowing position). | Resolved (parser; docs/13 §4 candidate) |
| G-24 | §13.4's error-kind exemplar writes `kind file problem⏎    is missing with path of type text`, but §7.12's normative declaration form (and its own exemplar, `is a blank`) starts every kind variant with the article-led `is a <variant>` — the article is how a variant line stays distinguishable from a `has`-style field clause and how construction (`a missing with …`) reads as the same article. The two exemplars disagree on the article. | 00 §13.4 vs §7.12 | Examplar-vs-exemplar tension (the same class as G-11/G-21) | §7.12's normative form wins: every variant line is `is a <variant> [with …]` — so §13.4's kind reads `is a missing with path of type text`, and construction/matching use the same article (`a missing with path f`, `when a missing with path f`). The §13.4 exemplar is flagged for the one-word correction at the next doc pass; the parser keeps its single `is a` variant rule (no grammar change). Pinned by sema corpus test `m1_corpus_error_kinds_are_ordinary_kinds`. | Resolved (parser; docs/13 §4 candidate; §13.4 exemplar flagged) |

| G-23 | §26.2 names three diagnostic verbosity modes (`student`/`normal`/`expert`) and freezes student (the five-part teaching format) and expert (the terse code-first one-liner) exactly, but gives `normal` no rendered shape, and the doc-09 stub defers the per-project default-mode policy to M1 with user data. | 00 §26.2 vs docs/09 (stub) | Underspecification (one of the three modes has no render spec) | `normal` renders the teaching format's what/where/why — the student format minus the `To fix, write:` footer and the `Learn more:` concept link (the parts a mature reader needs least; both remain in student mode). No rendering is silently changed: student stays the default for `lagom new` projects and for the CLI today; the mode is selectable per invocation (`--student`/`--normal`/`--expert`) and per project (`Lagom.toml` `diagnostics = "normal"`). doc 09's open question (auto-escalation) stays open as written. Pinned by diagnostics tests rendering one diagnostic in all three modes. | Resolved (diagnostics/driver/cli; docs/13 §4 candidate) |
| G-25 | D-35 (00 §31.8, doc 11 R-17) locked "trap in release" as the recommendation and scheduled the M1 numeric-suite benchmark as its decision datum (threshold: >2% on the numeric suite revisits with data). The datum had not been measured. | 00 §31.8/D-35, doc 11 R-17 | Missing decision datum (scheduled by the roadmap, not a language contradiction) | Measured 2026-09-12 (Windows x86-64, release build, 3M-iteration mixed integer suite `+ − × ÷⌊ rem` over lists/repeat — `validation/d35_overflow.lagom`): trap vs wrap binaries differ by **1.0% wall-clock** (trap 1539.5 ms mean, n=13 vs wrap 1524.2 ms mean, n=10, paired interleaved runs, sd ≈ 50 ms each; identical printed results, overflow never triggered by the workload). Below the 2% revisit threshold: **D-35's trap-in-release recommendation stands as locked**. The wrap variant was a temporary `rt_binop_wrap_d35` + `LAGOM_D35_WRAP=1` codegen hook, removed after the measurement. | Resolved (benchmark measured; 00 §31.8 may now freeze) |
| G-26 | §13.1 says `and pass the problem on` is "the readable `?`", but the attempt statement produces no value the following statements can read — the success value vanished (the demo transcript `attempt parse age … and pass the problem on` + a later read of the parsed value is undervivable). | 00 §13.1 ("the readable `?`") vs the propagate statement's void lowering | Underspecification (the `?` reading implies value flow) | The readable `?` forwards the success value: after `attempt <call> and pass the problem on`, the fixed name `result` is bound to the success value on the continuation path (same fixed-name mechanism as the `if it fails` form, 13.1's own note; propagation short-circuits so `result` exists only on success). Sema declares `result`; MIR binds the attempt's success local and continues in the same block. Pinned by interp+native tests (`pass_on_binds_result`). | Resolved (sema/mir; docs/13 §4 candidate) |
| G-27 | §7.6/§7.8's exemplars `make larger equal to bigger of a and b` (and §7.8's "bigger(a, b) — the maximum") name `bigger`/`smaller` as module functions, but no module or registration implements them — the demo's flowing-calls section is undervivable, and no kernel builtin carries the name. | 00 §7.6/§7.8 vs the stdlib registration | Exemplar-vs-implementation gap (an exemplar is normative; the surface was never wired) | `bigger of a and b` and `smaller of a and b` are the max/min functions of the `math` module (§7.8's own "bigger(a, b) — the maximum" reading; `use math` required like `square root`). Two numbers in, the larger/smaller one out (decimal promotion follows S-3). They lower to a Max/Min binary instruction; both backends share the rt `binop` table row. No new syntax: they ride the closed call grammar's `of`/`and` forms. Pinned by sema + interp + native tests. | Resolved (sema/mir/lir/rt/interp; docs/13 §4 candidate) |
| G-28 | The frozen spec spells the return statement `give back` (00 §7.8's clause list, §7.14, §7.15's `giveback` production, §33's cross-language table), but the implementation shipped `gives back` (lexer phrase-token `["gives", "back"]`, all validation programs, demo, tests, and the docs extracts edited to match during M1) — a keyword spelling changed in code and docs without a decision record in this table. Found by the M1 audit. | 00 §7.8/§7.14/§7.15/§33 vs the shipped lexer | Examplar-vs-implementation divergence (audit finding; not silently re-decided now) | The shipped `gives back` stands: it is verb-agreement-consistent with the sibling clauses `takes`/`returns`, matches every compiled program since M0, and dual-spelling would add undocumented ambiguity to a closed keyword set. Recorded here because it predates the audit and must not be reverted silently either. The cross-language table row (00 §33, `give back` → `return`) reads identically either way. Pinned by the lexer's own phrase-token tests (`kws("gives back total")` → `GiveBack`). | Resolved by record (audit 2026-09-13; no code change) |
